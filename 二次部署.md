# 添加执行权限
chmod +x deploy.sh

# 只部署后端
./deploy.sh backend

# 查看服务状态
./deploy.sh status


# 停止并删除容器（但保留 volume 数据）
docker compose down

# 重新构建并启动（不使用缓存）
docker compose up -d --build 

docker compose up -d --build backend

docker compose up -d --build frontend

docker compose up -d --build backend frontend